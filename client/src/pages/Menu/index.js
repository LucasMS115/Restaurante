import React from 'react';
import './styles/styles.css';

const menu = (props) => {
    return(
        <div id="title">
            <h1 className="t3">Cardápio</h1>
        </div>
    );
};

export default menu;