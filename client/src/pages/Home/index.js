import React from 'react';
import './styles/styles.css';
import Header from '../../components/Header';

const home = (props) => {
    return(
        <div id="title">
            <Header/>
        </div>
    );
}

export default home;